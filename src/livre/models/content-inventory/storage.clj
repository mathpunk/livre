;; Now On MongoTV
;;
;; 1. read configuration for mongodb address
;; 2. connect to mongodb
;; 3. connections are a value
;; - bind to units: the 'in db'
;; - bind to products: the 'out db'
;;
;; *. don't sweat users; they interact with the db through the server. 
;;
;; 
